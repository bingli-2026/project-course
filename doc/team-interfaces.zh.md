# 团队接口

> 英文版本:[`team-interfaces.md`](team-interfaces.md) (以英文版为准,中文版用于队内交流)

一页纸说清楚谁产什么、谁用什么、东西放仓库哪个目录,避免**默默撞车**(两个人在做同一件事)和**默默漏掉**(以为别人在做但其实没人做)。

下面的分工沿用开题报告里的人员表。**如果谁觉得自己的范围写错了,在动手写代码前提出来** —— 现在改比集成的时候改便宜得多。

---

## 总览图

```
┌─────────────────────────────────────────────────────────────────────────┐
│  硬件 / 采集                                                            │
│  ─────────                                                              │
│  电机 + 减速箱  ──►  ADXL345  ──►  STM32  ──►  Orange Pi                │
│   (甘芸瑄,         (传感器)     (采集 + 控制   (上位机,                │
│    杨喆)                          + 驱动)       冶秉礼)                 │
│                          相机  ──────────────►                          │
│                       (冶秉礼)                                          │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │ 原始视频 + 原始传感器流
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  特征提取(离线 / 在 Orange Pi 上)                                     │
│  ─────────────────────────────                                          │
│  视觉处理                          传感器处理                           │
│   (张展硕)                          (待定 — 见下方 Open Q)              │
│   产出 vision_* 特征                产出 sensor_* 特征                  │
│   每个样本一个 CSV                  每个样本一个 CSV                    │
│                                                                         │
│   都写到:  data/samples/<sample_id>.csv                                │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │ 特征 CSV / Parquet
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  存储 + 展示(Web 栈)                  建模(离线)                    │
│  ───────────────────                    ──────────                      │
│  Web 后端(FastAPI)                    模型训练                        │
│   (贺鑫皓) — 本仓库                      (杨炫志)                       │
│   入库、索引、通过                        消费 data/samples/,           │
│   /api/v1/samples 提供服务                训练分类器,                   │
│                                          (后期)把预测回写到 CSV         │
│  Web 前端(React/Vite)                                                  │
│   (贺鑫皓)                                                              │
│   列表 / 详情 / 图表                                                    │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
                             文档与测试
                             (张嘉欣)
```

---

## 每人契约

### 冶秉礼 — 导师对接、设备驱动、相机数据采集
- **产出**:原始相机视频文件;STM32 ↔ Orange Pi 通信驱动
- **消费**:来自甘芸瑄 / 杨喆 的硬件
- **交付给**:张展硕(视觉处理需要原始视频)
- **仓库位置**:`src/project_course/camera/`(已有相机 CLI 工具)
- **关于"back-end driver"**:这里指**设备侧驱动软件**(STM32 到 Orange Pi 之间),**不是** Web 后端。为避免混淆,在 commit 信息和聊天里建议用 *"acquisition driver / 采集驱动"* 这个说法。

### 张展硕 — 视觉数据处理
- **产出**:每个样本一个 CSV,包含 `vision_*` 列,符合 [`data-contract.zh.md`](data-contract.zh.md) 和 [`feature_schema.md`](feature_schema.md)
- **消费**:冶秉礼采集的原始视频
- **交付给**:贺鑫皓(把文件丢进 `data/samples/`)+ 杨炫志(同一份文件)
- **仓库位置**:视觉处理脚本当前在 `laboratory/` 下;长期应迁移到 `src/project_course/vision/`

### 传感器处理 — 贺鑫皓 + 杨炫志(2026-05-11 确认共同负责)
- **产出**:进程内的 `WindowSample` payload,`sensor_*` 字段已填,通过 `live.publish_window` 推送
- **消费**:冶秉礼的采集驱动产出的 STM32 → 香橙派传感器流
- **仓库位置**:目标在 `src/project_course/fusion/imu_features.py`(`specs/.../tasks.md` T018 已规划)

### 杨炫志 — 模型训练
- **产出**:训练好的模型文件(根据 2026-05-10 与贺鑫皓的澄清,**不**由 Web 后端提供推理服务;模型跑在边缘端)
- **消费**:`data/samples/*.csv`(和仪表盘读的是同一批文件)
- **交付给**:Orange Pi 上的边缘运行时
- **可选(后期)**:把 `predicted_label` / `prediction_confidence` 列回写到 CSV(数据契约里已预留)
- **仓库位置**:暂定 `src/project_course/modeling/`

### 贺鑫皓 — Web 前后端(本仓库)
- **产出**:`http://localhost:5173` 的 Web 仪表盘 + `http://localhost:8000/api/v1` 的 REST 接口
- **消费**:`data/samples/*.csv`(只读)—— **不**回写
- **仓库位置**:`src/project_course/api/`、`frontend/dashboard/`
- **不负责**:模型推理、实时流、增量学习、原始视频的存储

### 甘芸瑄 / 杨喆 — 硬件
- **产出**:能跑的物理装置(电机 + 减速箱 + 加速度计 + STM32 板);BOM / 接线文档
- **消费**:经费
- **交付给**:冶秉礼(把硬件交给他去采集数据)
- **仓库位置**:硬件笔记、原理图、BOM 待新建 `doc/hardware/`

### 张嘉欣 — 文档与测试
- **产出**:开题 / 中期 / 结题报告;测试计划;答辩 PPT
- **消费**:其他所有人的产出
- **仓库位置**:`doc/`

---

## 文件系统边界

| 路径 | 拥有者 | 谁会读 |
|---|---|---|
| `src/project_course/api/` | 贺鑫皓 | — |
| `frontend/dashboard/` | 贺鑫皓 | — |
| `data/samples/` | 特征生产方(张展硕 + 传感器负责人) | 贺鑫皓(Web BE)、杨炫志(模型) |
| `data/project_course.sqlite3` | Web BE 自动生成 | —(不要手动改) |
| `src/project_course/camera/` | 冶秉礼 | — |
| `laboratory/` | 张展硕(视觉原型) | 仅供参考 |
| `doc/` | 全员(走 PR review) | 全员 |

**不要直接写 `data/project_course.sqlite3`。**它是后端每次启动时根据 `data/samples/` 重建的索引文件。要清空就直接删掉它,不会有事。

---

## 已决议事项(2026-05-11 群里答复)

5 个阻塞问题全部答完。原始问题留存在 [`team-alignment-questions.zh.md`](team-alignment-questions.zh.md)(已归档)。

1. **传感器侧特征提取由谁做?** —— **贺鑫皓 + 杨炫志 共同拥有**,跑在 Web 后端同一个 Python 进程。上面 §"传感器处理 — 暂无人接手"那一节已经过期。

2. **冶秉礼的 "back-end driver"** —— 确认是**设备侧采集驱动**(STM32 ↔ 香橙派数据链 + 相机接入),**不是** Web 后端。STM32 之上到 Python 的接口由杨喆负责。

3. **中期演示模型部署在哪** —— **边缘节点(香橙派)**,和 Web 后端在同一 Python 进程内。中期不做独立的 gRPC/REST 服务。后续要独立部署也可以,数据契约不会变。

4. **Label 词表** —— 采用 `normal / unbalance / loose / misaligned / unknown`。闭集,小写 ASCII,严格拼写。扩展走版本化 PR,中期前不改主词表。详见 [`data-contract.zh.md`](data-contract.zh.md) §6。

5. **窗口长度** —— `window_size_s = 0.5`,`window_hop_s = 0.25`。两种模态共用。详见 [`data-contract.zh.md`](data-contract.zh.md) §7。Web 后端不持久化原始滑窗数组;特征在边缘端计算后通过 `live.publish_window` 推到 Web BE。

## 角色契约的变更

群里答复让上面"每人契约"小节有三处变动:

- **传感器处理 — 暂无人接手** → 现在由**贺鑫皓 + 杨炫志共同负责**,在香橙派上和 Web BE 同进程
- **杨炫志 — 模型训练** → 还负责**边缘端模型推理**,在同一 Python 进程内。预测结果通过 `live.publish_window({..., "predicted_state": ..., "prediction_confidence": ...})` 写回实时 buffer,中期不走独立 HTTP 服务
- **贺鑫皓 — Web 前后端** → Web BE 把 `live.publish_window` 和 `live.record_sync_quality` 作为给特征 / 模型代码用的进程内 API 暴露出来;HTTP 路由仍然只是 buffer 的只读消费方
